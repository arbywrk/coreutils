# Basic principles I follow while writing this utils

1. 'version' and 'help' flags have no short options
2. 'R' means recursive => no small r for recursion
3. 'q' and 'quiet' surpress all output (for scripts when they want to use the tool as a boolean)